(function () {
    'use strict';

    angular
        .module('users', [
            'users.controllers',
            'users.services',
            'users.directives'
        ]);

    angular
        .module('users.controllers', []);

    angular
        .module('users.services', []);

    angular
        .module('users.directives', []);

})();